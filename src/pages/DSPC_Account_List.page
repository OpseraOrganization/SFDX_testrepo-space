<apex:page standardStylesheets="true" tabStyle="Accounts__tab" controller="DSPC_Acclist_controller">
<script type="text/javascript"> 
    var previousOnload = window.onload; window.onload = function() { 
    if (previousOnload) { previousOnload(); } 
    var elements = document.getElementsByClassName('actionColumn');
    for(var i = 0, length = elements.length; i < length; i++) {
          elements[i].style.display = 'none';      
    } 
    } 
</script>
  <!--  <apex:image id="theImage" value="{!$Resource.DSPC_Account_Home}"/> -->
    <apex:form >
     <apex:pageBlock id="myblock">        
            <apex:pageBlockButtons location="Top">
                <apex:commandButton value="Request New Account" action="{!New_form}"/>
                <apex:commandButton value="Account Request Status" action="{!AccountRequestStatus}"/>
            </apex:pageBlockButtons>
     </apex:pageBlock>
     </apex:form>
 <!--   <div style="overflow:auto; width:80%">
    <apex:form id="formOne">    
    <div style="position:relative;">
    <apex:outputpanel >
        <apex:actionstatus id="status">
            <apex:facet name="start">
                <div class="waitingSearchDiv" id="el_loading" style="background-color: #FFFFFF; height: 100%; width:100%; opacity: 0.95">
                    <div class="waitingHolder" style="top: 74.2px; width: 100%; background-color: #FFFFFF;">
                        <img class="waitingImage" src="{!$Resource.DSPC_Loading}" title="Please Wait..."/>
                    </div>
                </div>
            </apex:facet>
        </apex:actionstatus>
        <apex:pageBlock id="myblock">        
            <apex:pageBlockButtons location="Top">
                <apex:commandButton value="Request New Account" action="{!New_form}"/>
                <apex:commandButton value="Account Request Status" action="{!AccountRequestStatus}"/>
            </apex:pageBlockButtons>
            <div style="overflow:scroll; width:100%">
            <apex:pageblocktable id="tableOne" value="{!AllSearchacts}" var="a">
                <apex:column headerValue="Account Name"><a href="/dspc/{!a.id}">{!a.name}</a></apex:column>
                <apex:column value="{!a.Denied_Party_Alert_formula__c}"/>
                <apex:column value="{!a.Strategic_Business_Unit__c}"/>
                <apex:column value="{!a.CBT__c}"/>
                <apex:column value="{!a.CBT_Team__c}"/> 
                <apex:column value="{!a.CBT_Directorate__c}"/>
                <apex:column value="{!a.type}"/> 
                <apex:column value="{!a.OwnerId}"/>
                <apex:column value="{!a.Region_Name__c}"/>
                <apex:column value="{!a.Sbl_Account_Row_Id__c}"/>
                <apex:column value="{!a.D_S_Market_Org__c}"/>
                <apex:column value="{!a.ParentId}"/>
                <apex:column value="{!a.Top_Account__c}"/>                 
            </apex:pageblocktable> 
            </div>
        <apex:panelGrid columns="2">
    <apex:commandlink action="{!con.previous}" status="status" rerender="myblock" rendered="{!hasPrevious}" value="< Previous"/>
    <apex:commandlink action="{!con.next}" status="status" rerender="myblock" rendered="{!hasNext}" value="Next >"/>
    </apex:panelGrid>
    </apex:pageBlock>
    </apex:outputpanel>
    </div>
    </apex:form>
    <apex:ListViews type="Account" />
     </div> -->
     <apex:enhancedlist type="Account" height="730" customizable="false" rowsPerPage="25" Listid="00B30000006uSwR" />
</apex:page>