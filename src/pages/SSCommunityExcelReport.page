<apex:page ></apex:page>
<!--<apex:page controller="SSCommunityCases_Cntrl" contentType="application/vnd.ms-excel#SS_CommunityExport_Results.xls" standardStylesheets="false" cache="true" showHeader="false" sidebar="false">
    <apex:pageBlock title="Export Results" > 
        <apex:pageblockTable value="{!CaseExportList}" var="SS" border="0.1px">
             <apex:column > 
                <apex:facet name="header">Case Number</apex:facet>
                <apex:outputText value="{!SS.CaseNumber}"/>
            </apex:column>
            <apex:column value="{!SS.CreatedDate}"/> 
            <apex:column value="{!SS.ClosedDate}"/>
            <apex:column value="{!SS.Subject}"/>
            <apex:column value="{!SS.Description}"/>
            <apex:column value="{!SS.Resolution__c}"/>
            <apex:column headerValue="Case Owner">
                <apex:outputtext value="{!SS.Owner.Name}"/>
            </apex:column>
            <apex:column value="{!SS.Status}"/> 
            <apex:column style="text-decoration: none" value="{!SS.Aircraft_Type__c}"/>
            <apex:column headerValue="Supported Product">
                <apex:outputtext value="{!SS.Supported_Products__c}"/> 
            </apex:column>             
            <apex:column value="{!SS.ATA_Chapter__c}"/>
            <apex:column headervalue="Case Comments"> 
                <apex:repeat value="{!SS.CaseComments}" var="c">
                    <apex:outputText value="{!c.CommentBody}"/><br/>
                </apex:repeat>
            </apex:column>
            <apex:column headerValue="Comments Created By" >
                <apex:repeat value="{!SS.CaseComments}" var="c">
                <apex:outputtext value="{!c.CreatedBy.Name}"/><br/>
                </apex:repeat>
            </apex:column>
        </apex:pageblockTable>
    </apex:pageBlock> 
</apex:page>-->