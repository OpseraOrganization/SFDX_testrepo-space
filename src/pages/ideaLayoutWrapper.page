<apex:page controller="IdeaLayoutController" standardStylesheets="true" showHeader="false" cache="{!doCache}" expires="{!cacheMaxAge}"  action="{!redirectToOverride}">

    <apex:include pageName="{!layoutOverride}" rendered="{!isLayoutOverridden}" />
    <apex:include pageName="ideaLayout" rendered="{!!isLayoutOverridden}" />

</apex:page>