@isTest
class globalTest{

  static testMethod void ccTest(){
     codecoverage.ccMethod();
     //repeat the same for the rest of classes.
  }  
  static testMethod void ccTest1(){
     codecoverage.ccMethod2();
     //repeat the same for the rest of classes.
  }  
    static testMethod void ccTest2(){
     codecoverage.ccMethod3();
     //repeat the same for the rest of classes.
  }
    static testMethod void ccTest3(){
     codecoverage.ccMethod4();
     //repeat the same for the rest of classes.
  }
}