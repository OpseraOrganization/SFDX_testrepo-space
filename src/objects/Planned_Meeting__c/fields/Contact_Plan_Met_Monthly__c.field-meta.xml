<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Contact_Plan_Met_Monthly__c</fullName>
    <externalId>false</externalId>
    <formula>IF(  OR(ISNULL(Completed_Date__c), ISNULL(Expected_Date__c)),
        null,
	    IF((Completed_Date__c &lt;= Expected_Date__c)  , 
	        1, IF(((MONTH(Completed_Date__c) = MONTH(Expected_Date__c)) &amp;&amp; (YEAR(Completed_Date__c) = YEAR(Expected_Date__c))),1,0
                  )
          )
 )</formula>
    <label>Contact Plan Met (Monthly)</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
