<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>GDMFSL_Location_ActiveWO__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>This property will be filled with the location value derived from WorkOrder.Location upon: Creation of the WorkOrder and Update of Location field on the WorkOrder and the value will be removed in case of the WO being closed (status = Complete/Incomplete/Canceled). It serves to show a related list on the Location object (current default WO list is not available as related list).</description>
    <externalId>false</externalId>
    <label>Location Active WO</label>
    <referenceTo>Location</referenceTo>
    <relationshipLabel>Work Orders (Active)</relationshipLabel>
    <relationshipName>Work_Orders1</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
