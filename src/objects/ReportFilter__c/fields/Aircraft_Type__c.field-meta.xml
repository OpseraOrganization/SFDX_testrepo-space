<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Aircraft_Type__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Are you concerned with a particular aircraft type (platform name)?  If so, which one(s)?</description>
    <externalId>false</externalId>
    <inlineHelpText>Are you concerned with a particular aircraft type (platform name)?  If so, which one(s)?</inlineHelpText>
    <label>Aircraft Type</label>
    <referenceTo>Platform__c</referenceTo>
    <relationshipLabel>ReportFilters</relationshipLabel>
    <relationshipName>ReportFilters</relationshipName>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
