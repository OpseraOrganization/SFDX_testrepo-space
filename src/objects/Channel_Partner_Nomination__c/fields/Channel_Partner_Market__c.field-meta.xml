<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Channel_Partner_Market__c</fullName>
    <externalId>false</externalId>
    <formula>substitute(
  IF(Includes(Account__r.Channel_Partner_Market__c, &quot;Business Aviation&quot;), &quot;Business Aviation;&quot;, NULL) +
  IF(Includes(Account__r.Channel_Partner_Market__c, &quot;Airlines / Cargo&quot;), &quot;Airlines / Cargo;&quot;, NULL) +
  IF(Includes(Account__r.Channel_Partner_Market__c, &quot;Missiles / Munitions / Space&quot;), &quot;Missiles / Munitions / Space;&quot;, NULL) +
  IF(Includes(Account__r.Channel_Partner_Market__c, &quot;Commercial Helicopters&quot;), &quot;Commercial Helicopters;&quot;, NULL) +
  IF(Includes(Account__r.Channel_Partner_Market__c, &quot;Defense&quot;), &quot;Defense;&quot;, NULL) +
  IF(Includes(Account__r.Channel_Partner_Market__c, &quot;Sensors / Industrial&quot;), &quot;Sensors / Industrial;&quot;, NULL),
    &quot;;.&quot;,

    NULL
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Channel Partner Market</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
