<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>User_OR_Contact</fullName>
    <active>false</active>
    <description>This validation rule is to prevent the user from entering either user or contact in a record</description>
    <errorConditionFormula>OR(and(User__c!=null,Contact__c!=null),and(User__c==null,Contact__c==null))</errorConditionFormula>
    <errorMessage>Please select either user or a contact for the opportunity  team</errorMessage>
</ValidationRule>
