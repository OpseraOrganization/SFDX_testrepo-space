<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Prevent_Product_Name_Change</fullName>
    <active>true</active>
    <description>Added to prevent Sales TS and Sales BD profile users from accidentally changing the Product Name</description>
    <errorConditionFormula>AND( 
ISCHANGED(Name), 
NOT(CONTAINS($Profile.Name, &quot;API&quot;)), 
NOT(CONTAINS($Profile.Name, &quot;Admin&quot;)), 
NOT(CONTAINS($Profile.Name, &quot;Analyst&quot;)) 
)</errorConditionFormula>
    <errorMessage>Cannot change Product Name - please contact a Salesforce Administrator to perform this action.</errorMessage>
</ValidationRule>
