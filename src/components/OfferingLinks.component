<apex:component controller="NotifySMEOnOffering" access="global">
  <apex:attribute name="offeringRecordID" assignTo="{!offeringID}" type="string" description="This is the offering id"/>
  <apex:repeat value="{!filePathMap}" var="key">
      <apex:outputLink value="{!filePathMap[key]}" id="theLink">{!key}</apex:outputLink><br/>
  </apex:repeat>
</apex:component>